REACT_APP_API_URL=http://localhost:8801

# Set to true to always use the offline sample dataset, even when the API is up.
# Left unset, the app calls the API and only falls back to the sample data when
# the request never reaches it (no response / timeout) — never on a 4xx/5xx.
REACT_APP_USE_MOCK=false
